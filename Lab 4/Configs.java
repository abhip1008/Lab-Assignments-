class Configs {
     // set dmode to false to compile out debug code
     public static final boolean DEBUG_MODE = false;
}

/**
 * I prefer using a GUI debugger because you can use step into and see values
 * at different points in time without adding more print statements.
 *
 * Print statements work well for smaller programs but for larger ones,
 * asserts and actual debuggers work better and more efficiently
 *
 * A GUI debugger is useful because it lets you pause runs, and
 * step through code line by line, seeing all the variables at that moment.
 *
 * With a debugger, you can inspect the call stack and variable values
 * at different points throughout the runs. Which is really hard to do
 * with just printing to the console
 *
 * Proper documentation helps make code easier to understand, which
 * helps reduce confusion and makes errors easier to fina and fix.
 */